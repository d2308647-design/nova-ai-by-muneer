# Nova AI Studio
Users never enter an API key. Keep OPENAI_API_KEY on the server.
Run: npm install && npm start
Set OPENAI_API_KEY as a server environment variable.
The video button is included but is not connected to a video provider in this starter.
